-# = form_for([@post, @post.complaints.build]) do |f|
-#   - if @complain.errors.any?
-#     #error_explanation
-#       %h2
-#         = pluralize(@complain.errors.count, "error")
-#         prohibited this complain from being saved:
-#       %ul
-#         - @complain.errors.full_messages.each do |message|
-#           %li= message
-#   .field
-#     = f.label :user_id
-#     %br/
-#     = f.number_field :user_id
-#   .field
-#     = f.label :post_id
-#     %br/
-#     = f.number_field :post_id
-#   .field
-#     = f.label :content
-#     %br/
-#     = f.text_area :content
-#   .actions
-#     = f.submit
= simple_form_for([@post, @post.complains.build]) do |f|
  = f.input :content, label: "Complain"
  = f.button :submit, class: "button"
